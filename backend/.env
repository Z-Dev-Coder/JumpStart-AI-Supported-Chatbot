SECRET_KEY=-v*j&3=u!x_anr5yx=67ypn=d25r5v4!iwmb$3$vbo5k1#zmo5
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database
DB_NAME=jumpstart_db
DB_USER=jumpstart_user
DB_PASSWORD=jumpstart2026
DB_HOST=localhost
DB_PORT=5432

# LLM — Ollama (local, or a tunnelled remote server e.g. Colab + ngrok)
OLLAMA_MODEL=qwen2.5:3b-instruct-q4_K_M
OLLAMA_BASE_URL=http://localhost:11434

# Skips the LLM groundedness safety layer for faster manual evaluation
# iteration on a slow local model. Set to False for real/final testing.
SKIP_LLM_SAFETY_CHECK=True



# JWT
ACCESS_TOKEN_LIFETIME_MINUTES=60
REFRESH_TOKEN_LIFETIME_DAYS=7

# Django Channels (use in-memory for dev, Redis for prod)
CHANNEL_LAYERS_BACKEND=channels.layers.InMemoryChannelLayer
